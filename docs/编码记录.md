- 2026-05-25

  1. 分析MEI工作时段，划分出2个成像时间段（地地转移阶段段、极轨探测阶段）
  2. 筛选了8组代表性数据（两日连续间隔1h成像），表示在4个不同开窗阶段内的近日点与远日点轨道数据（为了看最远与最近，一开始本来说用0.5-1这样的数值，好像不太符合这个数据，就没有用，不过也可以替换）
  3. **清洗出csv数据，用于缩放处理的输入（spo观测时间、对日距离）**

- 2026-05-26

  1. 先使用固定缩放因子值，单独对nc文件中的3个通道（171、193、304）的图像数据进行缩放

  2. 使用panoply查看结果，放大缩小图像看起来正常，但是有一页array值，原本为正常值，缩放后为NaN应该是数据出现问题（每个像素的辐射强度值，单位DN/s）

     【ps：发现nc文件本身带有dsun_obs数据，就不必与fits文件的距离挂钩了】

  3. 解决array没有值的问题【插值算法】，参照原始图像中边缘背景值大小，在缩小图像时用0.0或0.1填充数值；放大图像时根据原始像素位置上的强度重新估算新位置上的值（活动区的大亮度仍然存在，只不过被分配到更大的区域上了）

  4. 原先使用opencv，只能处理0-255，但是当前还涉及到array中值的处理（大量浮点数），转而使用scipy处理缩放（N为连续场插值，而非RGB图像缩放，对大动态范围更稳定）

  5. 用指定对日直线距离替换固定缩放因子值，添加缩放因子计算方法，将预测时间、处理时间、缩放因子大小等一系列参数添加进nc数据文件中

- 2026-05-29 **集成模块**

  1. 将清洗出的csv文件中的每条数据，输入进缩放处理中（需要考虑这一份nc文件如何映射为多份？提前做好时间匹配？直接重复使用后面再添加时间？**为载荷对太阳的成像视角或者载荷与太阳中心的连线的矢量预留接口，这个因素会影响成像纹理**